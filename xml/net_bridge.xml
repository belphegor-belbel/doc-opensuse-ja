<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect1 [
<!ENTITY % entities SYSTEM "generic-entities.ent">
%entities;
]>
<!-- [DOCU] Create a network bridge section in
                 administration guide doc
     -->
<sect1 xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:its="http://www.w3.org/2005/11/its" version="5.1" xml:id="sec-network-iface-bridge">
 <title>ネットワークブリッジデバイスの設定</title>
 <info>
  <meta name="description" its:translate="yes">ネットワークブリッジデバイスの設定</meta>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:translation>yes</dm:translation>
  </dm:docmanager>
 </info>

 <para>ネットワークブリッジとは仮想的なリンク層 (レイヤ 2) デバイスで、複数のネットワークセグメント同士を接続して、単一の論理ネットワークにするための仕組みです。ブリッジは仮想的なネットワークスイッチとして振る舞うので、流れるトラフィックを監視してハードウエア MAC アドレスをベースにした動的なフレーム転送を行います。一般的には、物理ネットワークデバイスを複数の仮想マシンで共有するような仮想化環境 (KVM や Xen など) での使用が考えられます。</para>
    <para>ネットワークブリッジは &yast; を利用してグラフィカルに設定できるほか、手作業で <filename class="directory">/etc/sysconfig/network/</filename> ディレクトリ内に設定ファイルを作成して設定する方法もあります。</para>
        <para>通常は YaST ネットワークデバイスモジュールを利用して設定するのがお勧めです。</para>
        <procedure xml:id="proc-network-bridge-yast-steps">
            <title>&yast; によるブリッジインターフェイスの作成</title>
            <step>
                <para>まずは <menuchoice><guimenu>&yast;</guimenu> <guimenu>システム</guimenu> <guimenu>ネットワーク設定</guimenu></menuchoice> を選択します。</para>
            </step>
            <step>
               <para><guimenu>追加</guimenu> を押し、 <guimenu>デバイスの種類</guimenu> で <guimenu>ブリッジ</guimenu> を選択したあと、 <guimenu>次へ</guimenu> を押します。</para></step>
            <step>
                <para><guimenu>設定名</guimenu> の欄にインターフェイス名 (例: <literal>br0</literal>) を入力して <guimenu>次へ</guimenu> を押します。</para>
            </step>
            <step>
                <para><guimenu>アドレス</guimenu> タブでは、まずネットワークブリッジインターフェイスの IP アドレス設定方法を選択 (<literal>DHCP</literal> もしくは <guimenu>固定 IP アドレス</guimenu>) し、後者であれば IP アドレスを設定します。</para>
            </step>
            <step>
                <para><guimenu>ブリッジ接続デバイス</guimenu> のタブに切り替えます。ここでは、ブリッジに接続する物理ネットワークカード、もしくは VLAN インターフェイスをチェックボックスで選択します。</para>
            </step>
            <step>
                <para><guimenu>次へ</guimenu> を押すとネットワーク設定の概要に戻ります。あとは <guimenu>OK</guimenu> を押すと、設定を保存してネットワークサービスが再起動されます。</para>
            </step>
        </procedure>

    <sect2 xml:id="sec-network-bridge-manual-generic">
        <title>手作業によるネットワークブリッジの作成</title>
        <para>&yast; を使用せずに手作業でネットワークブリッジを作成する場合、ネットワークディレクトリ内に各種の制御パラメータを記述したファイルを作成します。本章の例では、 <literal>br0</literal> という名前のブリッジインターフェイスを作成し、 <literal>eth0</literal> という物理インターフェイスを追加するまでの手順を説明しています。</para>
        <procedure xml:id="proc-network-bridge-manual-generic-steps">
            <title>wicked インターフェイス定義によるブリッジの設定</title>
            <step>
                <para>まずはネットワークブリッジデバイスの設定ファイルとして、新しい設定プロファイル <filename>/etc/sysconfig/network/ifcfg-br0</filename> を作成します:</para>
                <screen language="ini">STARTMODE='auto'
BOOTPROTO='static'
IPADDR='<replaceable>ブリッジに割り当てる_IP_アドレス</replaceable>'
BRIDGE='yes'
BRIDGE_PORTS='eth0'
BRIDGE_STP='off'</screen>
            </step>
            <step>
                <para>また、ブリッジに追加する物理ネットワークインターフェイスの設定も修正します。具体的には、 IP アドレスの設定があれば削除するほか、アドレスの自動取得の設定があれば、これを無効化して純粋な物理ポートとして動作するように設定します:</para>
                <screen language="ini">STARTMODE='auto'
BOOTPROTO='none'</screen>
            </step>
            <step>
                <para>固定 IP の設定を行う場合、必要であればデフォルトゲートウエイも明示的に設定します。デフォルトゲートウエイは <filename>/etc/sysconfig/network/ifroute-br0</filename> という別のファイルに記述します:</para>
                <screen language="text">default <replaceable>デフォルトゲートウエイの_IP_アドレス</replaceable> - br0</screen>
                <warning xml:id="war-network-bridge-generic-routes">
                    <title>ルータ設定の競合について</title>
                    <para>デフォルトゲートウエイを設定するにあたっては、あらかじめ物理インターフェイス (例: <filename>ifroute-eth0</filename>) 側で設定していないかどうかをご確認ください。物理インターフェイス側で設定してしまうと、カーネル内でルーティング競合フラグが設定され、期待通りに動作しなくなってしまいます。</para>
                </warning>
            </step>
            <step>
                <para>あとはネットワークの設定を再読み込みして、変更内容を反映させます:</para>
                <screen>&prompt.sudo;systemctl restart network</screen>
            </step>
        </procedure>
    </sect2>
   </sect1>
